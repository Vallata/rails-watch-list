<div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path @list.photo.key, height: 300, crop: :fill %>')">
  <h1> <%= @list.name %> </h1>
</div>

<%= link_to 'Back home', lists_path %>


<%# <%= @list.photo.attached? ? cl_image_tag(@list.photo.key, height: 300, width: 400, crop: :fill) : image_tag("https://upload.wikimedia.org/wikipedia/fr/2/25/Logo_de_Top-Chef.png")%>

<div class="container ">

  <div class="d-flex flex-row-reverse">
    <%= link_to "Add a bookmark", new_list_bookmark_path(@list), class:'btn btn-primary rounded-3' %>
  </div>

  <div class="row d-flex justify-content-between my-4">
    <% @list.bookmarks.each do |bookmark|%>
      <div class="col-5 p-3 m-3 border border-grey rounded-4">
        <div class="d-flex">
          <p class="mb-0 fw-bold"><%= bookmark.movie.title%></p>
          <p class="mb-0 mx-2">•</p>
          <p class="mb-0 text-secondary"><%= bookmark.movie.rating %></p>
          <i class="mt-1 text-warning mx-1 fa-solid fa-star fs-6"></i>
        </div>
        <p class="lh-1 fs-6 fst-italic text-secondary"><%= bookmark.movie.overview %></p>
        <div class="d-flex mb-1">
          <i class="p-2 fa-solid fa-quote-left"></i>
          <div class="p-2"><%= bookmark.comment%></div>
          <div class="ms-auto p-2"><%= link_to 'Delete', bookmark_path(bookmark), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}, class: 'text-decoration-none text-danger' %></div>
        </div>
      </div>
    <% end %>
  </div>

</div>
